---
navigationLabel: Home
---

import { format } from 'date-fns'
import { mix } from '@theme-ui/color'
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome'
import { faDev, faGithubSquare, faTwitterSquare, faLinkedin } from '@fortawesome/free-brands-svg-icons'
import { faEnvelopeSquare } from '@fortawesome/free-solid-svg-icons'

export const favTechs = ['React.js', 'Gatsby', 'JavaScript', '.NET Core', 'C#', 'Python', 'HTML5', 'CSS3', 'C', 'Java', 'SQL Server', 'MySQL',]

<Flex><Spinner height='30' /><h4>Requesting Introduction...</h4></Flex>

# helloWorld
<Message variant='success' mb={2}>
  <Text variant='styles.h4'>Hey Everyone, my name is Yash Dave!</Text>
  I develop awesome WebApps based on React.js, Gatsby and Pure HTML+CSS+JS. I am also an upcoming Data Scientist currently pursuing Python Profeciency.
</Message>

<Message variant='success' mb={2}>Dabbling into experimental cutting edge new technologies is my favorite pastime. I love motivating others to get into Open-Source and writing occasional blogs!</Message>
<br />
<Flex><Spinner height='30' /><h4>You seem fun to talk with, Hit me up! Fetching more information...</h4></Flex>

<Grid gap={2} columns={[2, '1fr 1fr']}>
  <Box>
    <Heading variant='styles.h2' sx={{mb:0}}>getInTouch</Heading>
    <Link href='mailto:mail2ypd@gmail.com' variant='styles.a' sx={{fontSize: '32px', mr: '3'}}><FontAwesomeIcon icon={faEnvelopeSquare} /></Link>
    <Link href='https://github.com/Amorpheuz/' variant='styles.a' sx={{fontSize: '32px', mr: '3'}}><FontAwesomeIcon icon={faGithubSquare} /></Link>
    <Link href='https://www.linkedin.com/in/Amorpheuz/' variant='styles.a' sx={{fontSize: '32px', mr: '3'}}><FontAwesomeIcon icon={faLinkedin} /></Link>
    <Link href='https://twitter.com/Amorpheuz/' variant='styles.a' sx={{fontSize: '32px', mr: '3'}}><FontAwesomeIcon icon={faTwitterSquare} /></Link>
    <Link href='https://dev.to/amorpheuz' variant='styles.a' sx={{fontSize: '32px'}}><FontAwesomeIcon icon={faDev} /></Link>
  </Box>
  <Box>
    <Heading variant='styles.h2' sx={{mb:0}}>favoriteTech</Heading>
    {favTechs.map((favTech, index) => {
        return (
          <Badge
            key={index}
            variant='primary'
            sx={{
              mr: 2,
              mb: 2,
              color: mix('secondary', 'success', `${index / favTechs.length}`),
              borderColor: mix(
                'secondary',
                'success',
                `${index / favTechs.length}`
              ),
            }}
          >
            {favTech}
          </Badge>
        )
      })
    }
  </Box>
</Grid>
<Heading variant='styles.h2'>fetchProjects</Heading>
<SourceList filter='projects'>
  {projects => (
    <Flex
      sx={{
        flexWrap: 'wrap',
        ml: theme => `-${theme.space[2]}px`,
        mr: theme => `-${theme.space[2]}px`,
      }}
    >
      {projects.splice(0, 3).map((edge, index) => {
        const {
          frontmatter: { title, date, tags },
          fields: { slug },
        } = edge.node
        return (
          <Box
            key={index}
            sx={{ mb: 3, maxWidth: ['100%', '50%', '33.333%'], width: ['100%', '50%', '33.333%'] }}
          >
            <Link href={slug} sx={{textDecoration: 'none'}}>
              <Card
                sx={{
                  ml: 2, 
                  mr: 2,
                  p: 3,
                }}
              > 
                <Text sx={{fontSize:0, color:'muted'}}>{format(new Date(date), 'd-MMM-u')}</Text>
                <Heading variant='styles.h4' sx={{color: 'text'}}>{title}</Heading>
                {tags.map((tag, index) => {
                  const { name, count, percent } = tag
                  return (
                    <Badge
                      key={index}
                      variant='primary'
                      sx={{
                        mr: 2,
                        mb: 2,
                        color: mix('success', 'secondary', `${index / tags.length}`),
                        borderColor: mix(
                          'success',
                          'secondary',
                          `${index / tags.length}`
                        ),
                      }}
                    >
                      {tag}
                    </Badge>
                  )
                })}
                <Text sx={{textDecoration: 'underline'}}>View project</Text>
              </Card>
            </Link>
          </Box>
        )
      })}
    </Flex>
  )}
</SourceList>
<Flex 
  sx={{
    justifyContent: 'center',
  }}
>
  <Button as='a' mr={2} href='/projects' variant='primary' >Show me more!! 👨‍💻👩‍💻</Button>
</Flex>
<Divider/>
<Heading variant='styles.h2'>fetchRecentActivity</Heading>

